Bootstrap: docker
From: almalinux:9.7

%labels
    Maintainer Mat3ra.com
    Version almalinux-apptainer-base-9.7-0

%help
    AlmaLinux base image containing GCC and other essential build tools.

%post
    # enable extra repos
    dnf in -y epel-release
    dnf config-manager --set-enabled crb

    dnf in -y autoconf \
        automake \
        bzip2 \
        cmake \
        gcc \
        gcc-c++ \
        gcc-gfortran \
        git \
        libtool \
        make \
        openssl-devel \
        python3 \
        python3-devel \
        rsync \
        tar \
        wget \
        which \
        zlib-devel

    # Clean up
    dnf clean all && rm -rf /var/lib/dnf /var/cache/dnf /var/cache/yum
